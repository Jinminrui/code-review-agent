appId: com.code-review-agent.app
productName: Code Review Agent

directories:
  output: release

files:
  - "package.json"
  - from: "packages/review-shell/dist"
    to: "dist"
  - from: "packages/review-backend/dist"
    to: "node_modules/@app/review-backend/dist"
  - from: "packages/review-shell/renderer"
    to: "renderer"

extraResources: []

asar: true

mac:
  target: dmg

win:
  target: nsis
