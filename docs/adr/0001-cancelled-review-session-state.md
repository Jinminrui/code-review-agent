# 使用 cancelled 表示用户可见的中止会话状态

用户主动停止的审查会话使用 `cancelled` 表示，abort 相关术语只用于底层执行机制。这样持久化的会话历史能与用户意图保持一致：中止会保留请求前已经提交的结果，并排除请求后才产生的问题，即使底层实现使用 `AbortController` 停止工作。一旦系统接受中止请求，审查会话最终收敛为 `cancelled`，即使剩余执行原本可以成功完成。
